import React from 'react';
import './AnalogClock.css';
type Props = {};
type AnalogClockProps = Props extends Record<string, never> ? React.FC<Record<string, never>> : React.FC<Props>;
export declare const AnalogClock: AnalogClockProps;
export {};
