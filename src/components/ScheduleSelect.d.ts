import React from 'react';
type Props = {};
type ScheduleSelectProps = Props extends Record<string, never> ? React.FC<Record<string, never>> : React.FC<Props>;
export declare const ScheduleSelect: ScheduleSelectProps;
export {};
