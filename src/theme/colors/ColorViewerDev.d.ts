import React from 'react';
type Props = {};
type ColorViewerDevProps = Props extends Record<string, never> ? React.FC<Record<string, never>> : React.FC<Props>;
export declare const ColorViewerDev: ColorViewerDevProps;
export {};
